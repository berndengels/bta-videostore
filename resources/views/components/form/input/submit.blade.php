<div class="form-group row">
    <div class="col-md-auto float-right">
        <input
            type="submit"
            id="{{ $name }}"
            name="{{ $name }}"
            value="{{ $value ?? '' }}"
            role="button"
            class="btn btn-primary col-md-auto px-5"
        />

    </div>
</div>
